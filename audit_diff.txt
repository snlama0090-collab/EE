diff --git a/audit_notifications.md b/audit_notifications.md
deleted file mode 100644
index ae3b4f1..0000000
--- a/audit_notifications.md
+++ /dev/null
@@ -1,21 +0,0 @@
-# Audit: notifications.php (driver) + related files
-
-## Findings
-
-1. `shrink` Subquery `IN (SELECT id FROM bookings WHERE user_id = ?)` can be replaced with a JOIN. `SELECT al.* FROM activity_logs al JOIN bookings b ON al.resource_id = b.id WHERE b.user_id = ? AND al.resource_type = 'booking'`. [sections/notifications.php:9]
-
-2. `shrink` `is_string($n['details']) ? substr($n['details'], 0, 80) : ''` is defensive bloat — `details` is always a nullable string from the DB. Replace with `substr($n['details'] ?? '', 0, 80)`. [sections/notifications.php:42]
-
-3. `shrink` Inline styles `style="font-size:12px;color:var(--muted-foreground);"` repeated 3× across all three notification files. Move to a CSS class `.notif-detail { font-size:12px;color:var(--muted-foreground); }` and `.notif-empty { text-align:center;color:var(--muted-foreground);padding:24px; }`. [sections/notifications.php:41,44,48, owner_sections/notifications.php:38,42, admin_sections/notifications.php:41,43,47]
-
-4. `shrink` `max-width:400px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;` inline in driver version but not in admin/owner — inconsistent. If needed, use a CSS class. [sections/notifications.php:41]
-
-5. `yagni` The notification dropdown in the header shows "No new notifications" static text — it never fetches actual counts. If the dropdown is always stale, remove it or make it dynamic. [driver.php:69]
-
-6. `delete` Stray `</write_to_file>` closing tag in owner_sections/notifications.php:50 — this is a template artifact, not valid PHP output. Remove it.
-
-7. `shrink` admin_sections/notifications.php uses `$db->query(...)` (no parameterization) while the other two use `prepare/execute` — inconsistent. [admin_sections/notifications.php:8]
-
-8. `shrink` Three nearly identical notification files (admin, owner, driver) — ~80% duplication. Could unify into a single partial with role-based query switching.
-
-net: -30 lines, -0 deps possible.
\ No newline at end of file
